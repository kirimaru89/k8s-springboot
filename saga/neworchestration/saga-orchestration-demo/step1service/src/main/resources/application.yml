server:
  port: 8081

spring:
  application:
    name: step1service

management:
  endpoints:
    web:
      exposure:
        include: "health,info,prometheus"
  endpoint:
    health:
      show-details: always 